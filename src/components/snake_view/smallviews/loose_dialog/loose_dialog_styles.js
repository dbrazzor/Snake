import {
	flexUtils
} from '../../../../utils/styles_utils';

const { center } = flexUtils;

export default ({
	usernameContainer: {
		height: 'auto',
		width: '100%',
		margin: [20, 0],
		...center
	}
});
